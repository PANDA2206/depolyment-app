apiVersion: v1
kind: ConfigMap
metadata:
  name: fashionflow-config
  namespace: fashionflow
  labels:
    app: fashionflow
    tier: api
data:
  LOG_LEVEL: INFO
  LLM_PROVIDER: mock
  AGENT_STRATEGY: multi_agent
  VECTORSTORE_PATH: /srv/app/.data/chroma
